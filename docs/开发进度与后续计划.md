# KotlinReader · 开发进度快照与后续计划

> **用途**：本文件是「中途交接」文档，供下次开工快速恢复上下文。**唯一进度真相源，MEMORY.md 已交叉引用**
> **快照时间**：2026-09-23 · 第三版（`:data:sources` 实现层已完成）
> **当前状态**：底层（core / domain / data）源码写完；`:app` + 6 个 `feature` 骨架与全局导航已落地；**`:data:sources` 实现层（规则引擎 / 书源 JSON / 健康度 / 离线缓存 Worker）已完成**；**工程整体仍未编译验证过**（本机无 Android SDK）。
>
> **本次会话变更（书源实现层）**：`:data:sources` 新增 11 个主源文件 + 4 个 JVM 单测；`domain` 新增 2 个端口（`SourceImportRepository` / `ChapterCacheScheduler`）；`core:model` 新增 `SourceRuleBroken` 与书源导入报表模型；`BookDao`/`BookRepository` 新增换源所需的 `rebindSource`。

---

## 0. 一句话结论

工程骨架已完整落地：**21 个已建模块（含 build-logic）/ 100+ Kotlin 文件 / 24 个构建脚本**。
下一步唯一阻塞项：在有 Android SDK 的环境完成首次 `:app:assembleDebug` 校错（按第 4 节清单核对）；随后进入 Phase B 书架 + 阅读器闭环。

---

## 1. 环境约束（重要）

| 项 | 现状 | 影响 |
| --- | --- | --- |
| JDK | 17，可用 | 满足 AGP 8.x 要求 |
| Android SDK | **未安装** | 无法 `assembleDebug` / `lint` / 单测，任何编译期问题尚未暴露 |
| Gradle 缓存 | **无**（首次构建需全量下载） | 首次 Sync 会拉取数百 MB 依赖，耗时较长 |
| 网络 | 可访问 Maven Central / Google | 依赖解析可行 |

**结论**：当前交付物是「未经编译验证的源码 + 构建配置」。首次真实编译必然报错，需预留一轮**编译校错**，重点核对**第 4 节** C-1~C-12 清单。

> 无 SDK 时也可做的先行校准（不需 Android 环境）：
> - `node scripts/build-archive.js` → 校验 docs 产物未过期；
> - 人工复核 `KrTheme`/`TypographyMapper`/`ContrastGuard` 等纯 Kotlin/Compose 代码的参数签名；
> - 检查所有新文件是否都有 koroFileHeader 文件头（第 7 节约定）。

## 1.1 现状盘点（2026-09-17 实际文件树）

| 目录 | 文件数 | 说明 |
| --- | --- | --- |
| `app/` | 13（5 kt / 6 xml / 1 kts / 1 pro） | 壳工程 + res（strings、themes、colors、矢量图标、自适应图标） |
| `feature/` | 18（6 kts / 6 xml / 6 kt） | 6 个模块 ×（构建脚本 + manifest + 占位 Screen） |
| `core/` | 48（41 kt / 7 kts） | 7 个 core 模块 |
| `data/` | 27（21 kt / 5 kts / 1 xml） | 5 个 data 模块（含 sources 骨架） |
| `domain/` | 18（17 kt / 1 kts） | 7 个仓库端口 + 10 个用例文件 |
| `build-logic/` | 12 | 7 个约定插件 + 注册脚本 + 版本目录 |

> 注意 Gradle wrapper（`gradlew` / `gradle-wrapper.jar`）**尚未生成**，有 SDK 环境后需先执行一次官方 `gradle wrapper` 或直接复用本机 Gradle 首次引导。

---

## 2. 已完成的模块清单

| 层 | 模块 | Kotlin 文件 | 状态 | 关键内容 |
| --- | --- | --- | --- | --- |
| 构建 | `build-logic` | 8 | ✅ | 约定插件：android.application / android.library / compose / hilt / room / serialization / jvm.library；已补 `Project.libs` VersionCatalog 访问器（原 .kt 插件类无法解析 `libs`，Sync 必炸），并删除了 `KrAndroidCommon` 中对 `CommonExtension` 的无效 `ExtensionAware` 强转（配置期 CCE 风险） |
| app | `:app` | 5 | ✅ 骨架 | `KrApplication`（@HiltAndroidApp）、`MainActivity`（@AndroidEntryPoint + edge-to-edge）、`KrRoutes` 路由表、`KrNavHost`（Navigation Compose：bookshelf / reader/{bookId} / search / sources / settings / about）、`KrApp`（底部页签 + 二级沉浸页区分）、proguard-rules、res（strings / themes / 自适应图标） |
| core | `core:model` | 14 | ✅ | `Book` / `Chapter` / `ReadingProgress` / `Bookmark` / `TypographyConfig` / `ReaderTheme` / `KrError` / `page/*`（PageSnapshot、PageStyleSpec、PaginationRequest、CachedChapter）/ `source/*`（BookSource、SearchModels）/ `import/*` / `parse/*` |
| core | `core:common` | 4 | ✅ | `KrDispatchers`、语义别名（IoDispatcher/Parse/DefaultDispatcher）、`runCatchingSpec`、`dedupHash`、`cheapHash` |
| core | `core:database` | 9 | ✅ | 7 张表实体（books / reading_progress / chapters / bookmarks / book_sources / chapter_cache / reading_sessions + book_groups）、`KrDatabase`、6 个 DAO |
| core | `core:datastore` | 5 | ✅ | 泛型 JSON 序列化器、TypographyStore / ThemeStore / ReaderPrefsStore / GlobalPrefsStore、`Sanitizer`（数值合法化）、DI 模块 |
| core | `core:network` | 3 | ✅ | OkHttp 供给、UA 轮换、域名级 `DomainRateLimiter` |
| core | `core:designsystem` | 4 | ✅ | `KrTheme`、阅读主题 `ContrastGuard`、排版 → TextStyle 映射、自定义字体解析 |
| core | `core:ui` | 3 | ✅ | Loading/Empty/Error 状态盒、通用弹窗、`BookCover`（含本地封面占位） |
| domain | `domain` | 17 | ✅ | 7 个仓库接口 + 端口（`ChapterPaginator` / `LocalBookReader` / `BookInfoExtractor` / `FileScannerGateway` / `ImportFileGateway` / `PdfPageRenderer` / `SourceRepository` / `SourceEngine` / `CacheRepository`）、12 个用例 |
| data | `data:parser` | 9 | ✅ | `CharsetDetector`、`CharOffsetIndex`、`ChapterRecognizer`、`TxtParser`、`EpubParser`、`PdfParser`（兼 `PdfPageRenderer`）、`LocalBookReaderImpl`、`BookInfoExtractorImpl`、`ParserModule` |
| data | `data:pagination` | 3 | ✅ | `TextMeasurerHolder`（桥接 Composition 与后台分页）、`PaginationEngine`（贪心装箱 + 章内字符偏移映射 + 缓存）、`PaginationModule` |
| data | `data:books` | 5 | ✅ | `FileArchive`（SAF→临时→私有归档/去重/空间探测）、`BookScanner`（File 优先 + DocumentFile 兜底）、`BookRepositoryImpl`、`ProgressRepositoryImpl`/`BookmarkRepositoryImpl`、`BookMappers`、`BooksDataModule`（含 Room 供给） |
| data | `data:settings` | 3 | ✅ | 4 个配置仓储实现、`FontRepositoryImpl`（魔数校验 + 私有目录 + 清单）、`SettingsDataModule` |
| data | `data:sources` | 11（+4 测试） | ✅ | `SourceJsonCodec` / `LegacySourceAdapter` / `SearchUrlTemplate`（书源 JSON 与第三方平铺字段兼容）、`SourceValidator`（三步校验 + 选择器预校验）、`SourceMappers` + `SourceRepositoryImpl`（Room 读写 + 健康度打点）、`SourceHttpClient`（限流/UA/Referer/退避/编码裁决）、`RuleEngine`（CSS+JsonPath+正则三类语法 + 后处理管道）、`SourceEngineImpl`（搜索/详情/目录/正文 + 换源）、`ChapterMatcher`（四级章节匹配）、`CacheRepositoryImpl`（LRU 淘汰）、`ChapterCacheWorker` + `ChapterCacheSchedulerImpl`（WorkManager）、`SourceImporter`（文本/URL/文件三入口）、`SourcesDataModule`（DI + EntryPoint） |
| feature | `feature:bookshelf` 等 ×6 | 6（每模块 1） | ✅ 骨架 | 每模块：build.gradle.kts（library+compose+hilt）+ manifest + 占位 Screen（参数只收数据 + lambda）；bookshelf 有临时入口冒烟路由，settings/about 串联 |

### 2.1 已落地的核心能力（对照第 4/5 卷）

| 档案章节 | 能力 | 落点 |
| --- | --- | --- |
| 4.2.1 | `BookParser` 统一四段契约（metadata / chapters / readChapter / close） | `core:model/parse/ParseModels.kt` |
| 4.2.2 | 编码探测（BOM → 候选枚举 → 往返校准） | `CharsetDetector.kt` |
| 4.2.2 | **字符偏移 ↔ 字节偏移稀疏索引**（批次锚点 + 精确定长 skip） | `CharOffsetIndex.kt` |
| 4.2.2 | TXT 流式分章（行长阈值 + 点线目录行排除 + 近距去重 + 前言回填 + 识别失败退化为全文） | `ChapterRecognizer.kt` / `TxtParser.kt` |
| 4.2.3 | EPUB：container.xml → OPF → manifest/spine → 封面 → HTML 净化 | `EpubParser.kt` |
| 4.2.4 | PDF：`PdfRenderer` 按页 PNG + 单章「全文」目录 + 加密/损坏静默降级 | `PdfParser.kt` |
| 4.3.2 | 分页：TextMeasurer 真实换行 → 贪心装箱 → `PageSnapshot` 携带 `start/endCharOffset` | `PaginationEngine.kt` |
| 4.4.2 | 三级进度写入（内存 / 去抖 / 强制落盘） | `SaveProgressUseCase.kt` + `ProgressRepositoryImpl` |
| 4.5.x | 排版 12 项参数落 DataStore + 8 套主题 + 自定义字体 | `core:datastore` + `data:settings` |
| 4.7.1/4.7.4 | 书源 JSON：三种包装 + 第三方平铺字段兼容 + 三步校验（字段/URL/选择器语法） | `SourceJsonCodec.kt` / `LegacySourceAdapter.kt` / `SourceValidator.kt` |
| 4.7.2 | 规则解释引擎：CSS 选择器 / JsonPath / 正则三类语法 + `trim|unescape|regex|replace|default` 后处理管道 + ReDoS 防护 | `RuleEngine.kt` |
| 4.7.3 | 五层反封禁前四层：按域限流、域名粘性 UA、Referer 伪装、失败退避（1s/2s）+ 编码裁决（BOM>meta>书源声明） | `SourceHttpClient.kt` |
| 4.8.1/4.8.2 | 聚合搜索与归并（并发 + 归一化去重） | `SearchAcrossSourcesUseCase.kt` / `ResultMerger.kt` / `SourceEngineImpl.search` |
| 4.8.3 | **换源**：四级章节匹配 + 进度迁移（只换章节下标，章内字符偏移原样保留） | `ChapterMatcher.kt` + `SourceEngineImpl.switchSource` + `BookRepository.rebindSource` |
| 4.8.4/4.8.5 | 离线批量缓存 + LRU 淘汰 | `ChapterCacheWorker.kt` / `ChapterCacheSchedulerImpl.kt` / `CacheRepositoryImpl.kt` |

### 2.2 本次会话（Phase A）新增 / 修改明细

**新增文件（30 个）**

| 模块 | 路径 | 作用 |
| --- | --- | --- |
| `:app` | `app/build.gradle.kts` | 壳工程依赖（6 feature + core + domain + Navigation/Hilt） |
| `:app` | `app/src/main/AndroidManifest.xml` | 权限最小化（仅 INTERNET）、`allowBackup=false` |
| `:app` | `app/src/main/kotlin/com/kr/reader/KrApplication.kt` | `@HiltAndroidApp` 依赖图根节点 |
| `:app` | `app/src/main/kotlin/com/kr/reader/MainActivity.kt` | `@AndroidEntryPoint` + `enableEdgeToEdge()` + `KrTheme` |
| `:app` | `app/src/main/kotlin/com/kr/reader/KrApp.kt` | Scaffold + 底部 4 页签，二级页隐藏页签 |
| `:app` | `app/src/main/kotlin/com/kr/reader/navigation/KrRoutes.kt` | 路由常量单一真相源 |
| `:app` | `app/src/main/kotlin/com/kr/reader/navigation/KrNavHost.kt` | NavHost，6 条路由，跨 feature 跳转全部走回调 |
| `:app` | `app/src/main/res/values/{strings,themes,colors}.xml` | 应用名、启动窗口主题、图标背景色 |
| `:app` | `app/src/main/res/drawable/ic_launcher_foreground.xml` + `res/mipmap-anydpi-v26/ic_launcher.xml` | 自适应图标（矢量，minSdk 26 无需位图回退） |
| `:app` | `app/proguard-rules.pro` | 保留 serialization / jsoup 反射依赖 |
| `:feature` ×6 | `feature/<name>/build.gradle.kts` + `src/main/AndroidManifest.xml` + `.../<name>/<Name>Screen.kt` | 占位页，参数只收数据 + lambda |
| `:data:sources` | `data/sources/build.gradle.kts` + `src/main/AndroidManifest.xml` | 骨架（此前 settings 已 include 但无目录 → Sync 必炸） |

**修改文件（4 个）**

| 路径 | 修改内容 | 原因 |
| --- | --- | --- |
| `build-logic/.../KrAndroidCommon.kt` | 新增 `Project.libs` VersionCatalog 访问器；删除对 `CommonExtension` 的无效 `ExtensionAware` 强转 | ① 约定插件 .kt 类不生成 `libs` 类型安全访问器，原 `target.libs` 全部无法编译 → build-logic 自身编译失败；② 该强转结果未使用且存在配置期 CCE 风险 |
| `data/pagination/build.gradle.kts` | 修正乱码注释 | C-12 注释巡检 |
| `core/datastore/build.gradle.kts` | 修正乱码注释 | C-12 注释巡检 |

### 2.3 本次会话（书源实现层）新增 / 修改明细

**新增文件（17 个）**

| 模块 | 路径 | 作用 |
| --- | --- | --- |
| `:data:sources` | `json/SourceJsonCodec.kt` | 书源 JSON 编解码，三种包装（对象/数组/`{"sources":[]}`） |
| `:data:sources` | `json/LegacySourceAdapter.kt` | 第三方（legado 风格）平铺字段 → `SourceRules`，含 `tag./class./id./@css:/@text` 选择器归一化与 `##` 替换表达式转 processing |
| `:data:sources` | `json/SearchUrlTemplate.kt` | 搜索 URL 模板：`url,{json}` 拆分、`|char=gbk|` 编码声明剥离、关键字编码渲染 |
| `:data:sources` | `SourceValidator.kt` | 三步校验 + Jsoup `Selector` 语法预校验 + 调试器复用 |
| `:data:sources` | `SourceMappers.kt` | 实体 ↔ 模型，rules JSON 列，损坏时降级空规则 |
| `:data:sources` | `SourceRepositoryImpl.kt` | Room 读写 + 健康度打点 + 按健康分排序 |
| `:data:sources` | `SourceHttpClient.kt` | 限流 / UA / Referer / 退避 / 编码裁决 / POST |
| `:data:sources` | `RuleEngine.kt` | 三类提取语法 + 后处理管道 + ReDoS 防护 + 正文抽取 |
| `:data:sources` | `SourceEngineImpl.kt` | 搜索/详情/目录/正文 + 多源对比 + 换源（进度迁移） |
| `:data:sources` | `ChapterMatcher.kt` | 换源四级章节匹配（精确/归一化/序号/比例） |
| `:data:sources` | `CacheRepositoryImpl.kt` | 章节缓存 + LRU 淘汰 |
| `:data:sources` | `ChapterCacheWorker.kt` + `ChapterCacheSchedulerImpl.kt` | WorkManager 批量缓存与调度（联网约束 + 退避） |
| `:data:sources` | `SourceImporter.kt` | 书源导入：文本 / 批量 URL / SAF 文件 |
| `:data:sources` | `di/SourcesDataModule.kt` | Hilt 绑定 + Worker EntryPoint |
| `:data:sources` | `src/test/...` ×4 | ChapterMatcher / SearchUrlTemplate / RuleEngine / SourceJsonCodec 单测 |
| `domain` | `SourceImportRepository.kt` / `ChapterCacheScheduler.kt` | 新增端口（保持 feature 不依赖 data 实现） |
| `core:model` | `source/SourceImportModels.kt` | 书源导入报表模型 |

**修改文件（5 个）**

| 路径 | 修改内容 | 原因 |
| --- | --- | --- |
| `core/model/.../KrError.kt` | 新增 `SourceRuleBroken` + `toAppError()` 分支 | AppError 是 sealed，data 层无法跨模块实现；用载体异常在 core 收敛为 `SourceRuleError` |
| `core/database/.../BookDao.kt` | 新增 `rebindSource(bookId, sourceId, bookKey)` | 换源需整体改写 books 表的唯一索引键 |
| `domain/.../BookRepository.kt` | 新增 `rebindSource(...)` | 换源契约 |
| `data/books/.../BookRepositoryImpl.kt` | 实现 `rebindSource`（先重绑主键再换目录） | 顺序反了会把书留在旧源上且目录已丢 |
| `data/sources/build.gradle.kts` | `core:database`/`core:network` 改 `api`；加 `work-runtime-ktx` 与测试依赖 | DI 图传递性 + Worker + JVM 单测 |

---

## 3. 尚未完成的部分（按开工优先级）

| # | 缺失项 | 说明 | 阻塞级别 |
| --- | --- | --- | --- |
| ~~1~~ | ~~`:app` 模块~~ | ✅ 2026-09-17 已建：manifest / res / KrApplication / MainActivity / 导航齐全 | 已完成 |
| ~~2~~ | ~~6 个 `feature` 模块~~ | ✅ 2026-09-17 已建骨架（build 脚本 + manifest + 占位 Screen） | 已完成 |
| ~~3~~ | ~~`:data:sources` 实现~~ | ✅ 2026-09-23 已完成：规则引擎 / 书源 JSON（含第三方兼容）/ 健康度 / 离线缓存 Worker / 换源；剩余 UI 侧（书源管理页、调试器、搜索页）属 Phase D | 已完成 |
| 4 | `:core:testing` | MEMORY.md 的 14 模块口径含 `core:testing`，但 `settings.gradle.kts` 未 include，且目录不存在（测试规则 / 断言工具） | P2（口径待对齐） |
| 3.1 | Gradle wrapper | `gradlew` / `gradle-wrapper.jar` / `gradle-wrapper.properties` 均未生成，无法保证团队统一 Gradle 版本 | P0（学院 env 就绪后第一条执行） |
| 5 | 单元测试与测试语料 | 第 7 卷要求 parser ≥ 85%、pagination ≥ 85%、sources ≥ 85% 覆盖率。目前仅 `data:sources` 有 4 个 JVM 测试类（ChapterMatcher / SearchUrlTemplate / RuleEngine / SourceJsonCodec）；**parser、pagination 仍为 0 测试** | P1 |
| 6 | CI 配置（GitHub Actions） | ktlint / detekt / 单测 / 构建 / 覆盖率门禁均未配置 | P1 |
| ~~7~~ | ~~res 资源~~ | ✅ app 已补 strings / themes / colors / 自适应图标矢量资源（feature 库模块无需资源） | 已完成 |

---

## 4. 已知待复核点（首次编译时优先核对）

> 这些不是确定的缺陷，而是「无法编译验证」情况下的**高风险代码片段**，首次 Sync 后应逐条对照。

| 编号 | 位置 | 关注点 |
| --- | --- | --- |
| C-1 | `data/pagination/…/PaginationEngine.kt` | `TextMeasurer.measure(...)` 在 Compose 1.6/1.7 的参数签名（尤其 `Constraints` 的 `androidx.compose.ui.unit` 包与 `maxLines`）可能与书写不一致 |
| C-2 | 同上 | 由 px 反解回 sp 依赖 `density.density * density.fontScale`；若 `rememberTextMeasurer` 自带 Density 与传入 `LocalDensity.current` 不一致，会导致排版与渲染不符（>1 屏偏差即触发 Gate 2 失败） |
| C-3 | 同上 | 缓存 key 含 `request.content.length` 但不含内容指纹，同章内容变化而长度相同的场景可能命中陈旧分页 → 建议改用 `cheapHash(content)` |
| C-4 | `core/database/dao/KrDatabase.kt` | 实体字段 / 索引 / ForeignKey 与实际 DAO SQL 列名是否逐一匹配（DAO 曾用「列名不一致」理由重写过一次，仍建议用 Room 的编译期校验兜底） |
| C-5 | `core/database/…/KrTypeConverters.kt` | 未被 `@TypeConverters` 显式注册到 Database 上时 Room 会直接编译失败 |
| C-6 | `data/parser/…/EpubParser.kt` | jsoup `dc|title` 命名空间选择器依赖 XML 解析模式；`EmupStructure.opfPath` 已修复但 XML 命名空间前缀写法需实机验证 |
| C-7 | `data/parser/…/PdfParser.kt` | `@Binds fun bindPdfPageRenderer(impl: PdfParser)` 要求 `PdfParser` 可被注入且为 `@Singleton`；同时 `openRenderer` 的 `return@openRenderer` 标签返回需确认无编译歧义 |
| C-8 | `core/datastore/…/JsonSerializer.kt` | DataStore 的 `OkioSerializer` 与 kotlinx-serialization 版本对齐（`kr.serialization` 插件版本需与 `:core:datastore` 实际 usage 一致） |
| C-9 | `build-logic` | ~~插件 ID 与 `libs` 访问器~~ 已修复：补 `Project.libs` VersionCatalog 扩展、删 `KrAndroidCommon` 无效强转；插件 ID 注册 7 个与各模块 `id("kr.*")` 引用一致。剩余：`libs.versions.toml` alias 名需在真实 Sync 后确认无歧义 |
| C-10 | `data/books/…/BooksDataModule.kt` | `fallbackToDestructiveMigrationOnDowngrade()` 名称与 Room 版本一致；同时 `DatabaseModule` 与 `DataStoreModule` 都放在 SingletonComponent，需排除重复 Provider |
| C-11 | `:app` / `:feature:*` | ~~模块不存在~~ 已建。剩余：Hilt `@AndroidEntryPoint` → ViewModel 注入链、`@ApplicationContext` 注入、`hiltViewModel()`（`androidx.hilt:hilt-navigation-compose`）**尚未端到端验证**；feature 模块当前无 ViewModel，Phase B 首个 ViewModel 落地时重点验证 |
| C-12 | 全工程 | 所有 `@Description` 注释一律使用了 koroFileHeader 格式；曾在生成过程中出现少量夹杂非中文的残缺注释，已修复若干，**仍建议在首次提交前全局扫一遍注释** |
| C-13 | `data/sources/…/SourceHttpClient.kt` | `HttpClientProvider.create(cacheDir, logEnabled)` 的形参名与签名需与 `core:network` 实际一致；`OkHttpClient.newBuilder()` + `callTimeout` 在 OkHttp 4.12 存在，但 `RETRYABLE_CODES` 用 `intArrayOf` 判 `response.code in ...` 需确认 `in` 对 IntArray 可用（可用，改为 `contains` 更稳妥） |
| C-14 | `data/sources/…/SourceRepositoryImpl.kt` | `SourceDao.recordSuccess(id, latencyMs, at)` 是「@Transaction + @Query」的 DAO 默认方法（非抽象方法），Room 2.6.1 支持但要求该方法是 `open`/非 final（Kotlin 接口方法默认可覆写，需确认生成的实现未被 final 化） |
| C-15 | `data/sources/…/di/SourcesDataModule.kt` | 使用 `dagger.hilt.EntryPoint` + `EntryPointAccessors.fromApplication`；`CacheWorkerDeps` 接口方法无参数返回类型即可，但需确认 Hilt 对「EntryPoint 接口 + @InstallIn(SingletonComponent)」的组合在无 `@AndroidEntryPoint` 场景下可生成实现 |
| C-16 | `data/sources/…/ChapterCacheSchedulerImpl.kt` | `workManager.getWorkInfosForUniqueWork(name).get(timeout, TimeUnit)` 返回 `ListenableFuture`（来自 work-runtime 传递依赖），**未显式声明该类型以避免 guava import**；若编译报找不到 `get`，改为 `kotlinx-coroutines` 的 `await()` 或 `asListenableFuture` 方案 |
| C-17 | `data/sources/…/json/LegacySourceAdapter.kt` | `JsonObject(emptyMap())` 构造与 `JsonPrimitive.booleanOrNull/intOrNull/contentOrNull` 需 kotlinx-serialization 1.7.3；`JsonObject.str()` 等私有扩展定义在 object 内，作用域正确但需确认 `BookSource` 的 `charset` 等字段在转换后都能通过 `SourceValidator` |
| C-18 | `data/sources` 单测 | 4 个测试类全部为纯 JVM（jsoup + Kotlin），**未执行过**（无 SDK/Gradle 环境）；`RuleEngineTest` 中 `regex:(a+)+=>x` 依赖 `isSafePattern` 判定，若实现判定逻辑调整需同步改断言 |

---

## 5. 后续开发计划（对应第 8 卷 WBS）

### Phase A · 让工程可编译（2026-09-17 已完成 1~3，4 待有 SDK 环境）

1. ✅ 建 `:app`：`kr.android.application` + compose + hilt、`AndroidManifest.xml`（allowBackup=false 呼应红线 2）、res（strings/themes/自适应图标）、`KrApplication`（`@HiltAndroidApp`）、`MainActivity`（`@AndroidEntryPoint`）。
2. ✅ 建 6 个 `feature` 模块骨架（build.gradle.kts + manifest + 占位 Screen，Screen 只收数据 + lambda）；✅ 顺带补齐 `:data:sources` 目录骨架（settings 已 include，原 Sync 必炸点）。
3. ✅ 补 `:app` 导航：Navigation Compose，路由定案 `KrRoutes`：bookshelf / reader/{bookId} / search / sources / settings / about；底部 4 页签（书架/搜索/书源/设置），阅读器与关于为沉浸式二级页。
4. ⬜ 首轮编译校错，消化第 4 节 C-1~C-12（C-9 已部分修复：`Project.libs` 访问器 + 移除无效 ExtensionAware 强转）。**这一步不做完，后面所有 UI 都无法验证。**

### Phase B · V0.1 阅读闭环（WBS 1.16 / 1.17 / 1.18）

| WBS | 任务 | 模块 |
| --- | --- | --- |
| 1.16 | 书架列表（懒列 + 封面占位 + 排序） | `feature:bookshelf` |
| — | 导入入口：挂 `ImportBooksUseCase`（SAF `OPEN_DOCUMENT` 单文件）与 `ScanFolderUseCase`（`OPEN_DOCUMENT_TREE` 目录扫描）两个入口 | `feature:bookshelf` |
| 1.17 | 阅读器：加载 → 分章 → 分页 → 上下滚动翻页 → 进度恢复 | `feature:reader` |
| 1.18 | 主题切换（默认 / 夜间）入口 | `feature:settings`（最小版） |

**Gate 1 验收**：5MB GBK TXT 分章 ≤ 2s；退出重进位置不变；零崩溃。

### Phase C · V0.2 排版与多格式（WBS 2.x）

- 四种翻页模式（覆盖 / 滑动 / 滚动 / 仿真），先做 **滚动 + 覆盖** 两种；
- 重分页触发链（去抖 + `mapLatest` + 锚点保持）——这是 Gate 2 的核心（12→36sp 偏差 ≤ 1 屏）；
- 排版设置面板 12 项参数实时预览；
- EPUB / PDF 接入已有解析器；
- 补齐 parser / pagination 单测到 ≥ 85%。

### Phase D · V0.3 书源闭环（WBS 3.x）

- ✅ **`data:sources` 数据层已全部完成**（2026-09-23）：`SourceJsonCodec` / `LegacySourceAdapter` / `SourceValidator` / `SourceRepositoryImpl`（含健康度）/ `SourceHttpClient`（反封禁四层）/ `RuleEngine` / `SourceEngineImpl`（搜索·详情·目录·正文·换源）/ `ChapterMatcher` / `CacheRepositoryImpl` / `ChapterCacheWorker` + 调度器 / `SourceImporter`；
- ⬜ `feature:sources`：书源管理页（列表/启用/分组/删除/健康度）+ 导入（粘贴 JSON / URL 列表 / 选文件）+ **规则调试器**（复用 `SourceValidator.checkSelector` 逐字段诊断）；
- ⬜ `feature:search`：聚合搜索 → 详情 → 加入书架 → 在线阅读（复用 `SearchAcrossSourcesUseCase`）；加入书架需走 `BookRepository.upsertImported`（网络书 `sourceId` + `sourceBookKey` 必填）；
- ⬜ 示例书源 JSON（档案 8.7.1 要求 3~5 个，放 `assets/sample_sources/`）。

### Phase E · V0.4/V1.0（WBS 4.x、5.x）

- 一键换源入口（数据层 `switchSource` 已就绪，只差 UI 触发与结果展示）；
- 正文净化流水线（domain 已有 `PurifyContentUseCase`，sources 已接入正文链路）、自定义主题 HEX、自定义字体；
- 正文净化流水线、自定义主题 HEX、自定义字体；
- 性能专项（第 6 卷 PF-01~PF-14）、内存 ≤ 150MB、i18n、无障碍、发布物料。

---

## 6. 下次开工的启动清单（照做即可）

### Step 1 · 恢复上下文（10 分钟）

```text
1. .workbuddy/memory/MEMORY.md              → 三条红线、线程模型、性能硬指标、模块划分
2. 本文件（读 0 / 1 / 2.2 / 4 / 5 节）        → 进度真相源
3. docs/04-第4卷-功能模块详细设计.md          → 要做的那个功能的算法级设计（书架看 4.6，阅读器看 4.2~4.4）
```

### Step 2 · 首编译校错（**最高优先级，未完成不要写 UI 业务代码**）

```text
前置：安装 Android SDK（compileSdk 35）+ JDK 17，首次 Sync 会拉取数百 MB 依赖
1. ./gradlew :app:assembleDebug      → 按第 4 节 C-1~C-12 逐条核对报错，切忌逐条猜错
2. ./gradlew :app:installDebug       → 冒烟：启动 → 书架 → 临时入口进阅读器 → 返回 → 底部页签互切
3. 每修一条就把第 4 节对应行改为「✅ 已验证 + 结论」，保持文档与代码同步（第 8 卷 8.4.4 纪律）
  预期首次报错集中在：Room 列名/TypeConverters（C-4/C-5）、TextMeasurer 签名（C-1）、
  OkioSerializer 版本对齐（C-8）、Hilt 重复 Provider（C-10）
```

### Step 3 · Phase B（V0.1 阅读闭环）开工顺序

| 顺序 | 任务 | 入口文件 | 依赖的 domain 能力 |
| --- | --- | --- | --- |
| 1 | 书架列表（懒列 + 封面占位 + 排序） | `feature/bookshelf/.../BookshelfScreen.kt` 改写；VM 用 `hiltViewModel()`（顺带验证 C-11） | `BookRepository.kt` |
| 2 | 导入入口（SAF 单文件 / 目录扫描） | 同模块，挂 Activity Result（`OPEN_DOCUMENT` / `OPEN_DOCUMENT_TREE`） | `ImportBooksUseCase.kt` / `ScanFolderUseCase.kt` |
| 3 | 阅读器：加载 → 分章 → 分页 → 滚动翻页 | `feature/reader/.../ReaderScreen.kt` 改写（当前是占位 Text） | `LoadChaptersUseCase` → `LoadChapterContentUseCase` → `PaginateChapterUseCase` |
| 4 | 字符级进度恢复与三级落盘 | 阅读器内部挂载 | `SaveProgressUseCase.kt`（三级写入已实现，只需接线） |
| 5 | 主题切换（默认/夜间）最小版 | `feature/settings/.../SettingsScreen.kt` 改写 | `SettingsRepository.kt` |

**注意**：占位 Screen 保留了一个「打开阅读器骨架」临时按钮，用于验证路由；第 3 步落地真实列表后应删除该按钮。

**Gate 1 验收**：5MB GBK TXT 分章 ≤ 2s；退出重进位置不变；零崩溃。

### Step 4 · 文档纪律

```text
每完成一个可观测阶段，更新本文件：① 第 2 节已完成清单 ② 第 3 节缺失项状态 ③ 第 4 节复核结论 ④ 顶部快照时间
修改的是 docs/ 分卷 → 必须跑 node scripts/build-archive.js（开发进度文件不参与该合并，可直接手改）
```

## 7. 约定提醒（写代码时必须遵守）

- **注释**：所有新文件都要 koroFileHeader 文件头；注释写「为什么」，禁止复述代码。
- **业务规则**：注释特定的东西解释业务妥协（如 `FileArchive.persistUriPermission` 解释为什么必须申请持久化授权）。
- **协程**：禁 `GlobalScope`；大循环插 `ensureActive()`；IO → `IoDispatcher`，分页 → `DefaultDispatcher` + `Mutex`。
- **大文件**：禁 `readText()` / `readBytes()`，一律流式（detekt `ForbiddenMethodCall` 会拦截）。
- **Compose**：参数只传数据 + lambda，动画用 `Animatable` + `graphicsLayer`，列表给稳定 key。
- **文档**：改了设计就要改 `docs/`，然后 `node scripts/build-archive.js` 重新生成根目录合并版与 HTML 版（**不要手改那两个生成产物**）。
